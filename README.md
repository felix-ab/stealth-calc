# Stealth Calc

A small PWA calculator interface with an optional OpenRouter-backed chat endpoint.

## Local Development

```bash
npm install
npm run dev
```

The API proxy expects `OPENROUTER_API_KEY` to be set in the deployment environment. No key is stored in the repository.
